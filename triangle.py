def area(a, b, c):
    # Принимает длины сторон треугольника, возвращает полупериметр треугольника
    return (a + b + c) / 2


def perimeter(a, b, c):
    # Принимает длины сторон треугольника, возвращает периметр треугольника
    return a + b + c
